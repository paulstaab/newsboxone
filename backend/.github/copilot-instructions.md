# headless-rss

The canonical repository instructions live in `AGENTS.md` at the repository root.

Use `AGENTS.md` as the primary document for:
- workflow and validation requirements
- runtime and API compatibility behavior
- documentation sync rules
- repository structure and troubleshooting notes

This file is intentionally kept as a compatibility shim so those instructions stay consolidated in one place.
